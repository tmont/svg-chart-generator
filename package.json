{
	"name": "svg-chart-generator",
	"description": "Generate simple SVG charts on the server",
	"keywords": [ "chart", "graph", "svg" ],
	"version": "0.0.1",
	"scripts": {
		"test": "node_modules/.bin/mocha -R spec tests"
	},
	"dependencies": {
		"extend": "3.0.0"
	},
	"devDependencies": {
		"should": "7.0.2",
		"mocha": "2.2.5"
	}
}
